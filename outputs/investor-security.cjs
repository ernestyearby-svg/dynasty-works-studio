const fs=require('fs'),path=require('path');const root='investor-deployment';const hits=[];let count=0;const patterns={credential:/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----|(?:gh[pousr]_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{40,}|sk-[A-Za-z0-9]{30,}|AKIA[A-Z0-9]{16})/,jwt:/eyJ[A-Za-z0-9_-]{15,}\.eyJ[A-Za-z0-9_-]{15,}\.[A-Za-z0-9_-]{15,}/,localPath:/[A-Z]:[\\/](?:Users|Program Files)[\\/]|\/Users\/[A-Za-z]/,privateNote:/Edgar Jimenez|NDA data|private investor/i};function walk(d){for(const ent of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules','.netlify','.openai'].includes(ent.name))continue;const f=path.join(d,ent.name);if(ent.isDirectory()){walk(f);continue}if(/^\.env/.test(ent.name))hits.push({file:f,type:'envFile'});if(!/\.(tsx?|jsx?|mjs|cjs|json|html|css|toml|md|txt)$/.test(f))continue;count++;const s=fs.readFileSync(f,'utf8');for(const [type,re]of Object.entries(patterns))if(re.test(s))hits.push({file:f,type});}}walk(root);fs.writeFileSync('outputs/investor-security.json',JSON.stringify({count,hits},null,2));console.log(JSON.stringify({count,hits}));
