import {useRef,useState} from 'react';
import {candidateSlugs,studies,studyHref} from './data';
export default function Shell(){const dialog=useRef<HTMLDialogElement>(null),trigger=useRef<HTMLButtonElement>(null);const[open,setOpen]=useState(false);const i=candidateSlugs.indexOf(location.pathname.replace(/\/$/,'').split('/').pop()!);function close(){dialog.current?.close();setOpen(false);trigger.current?.focus()}return <aside className="candidate-shell" aria-label="DWS experience navigation"><button ref={trigger} aria-expanded={open} aria-controls="candidate-navigation" onClick={()=>{dialog.current?.showModal();setOpen(true)}}>DWS / Navigate <span aria-hidden="true">＋</span></button><dialog ref={dialog} id="candidate-navigation" onClose={()=>{setOpen(false);trigger.current?.focus()}}><div className="candidate-menu-top"><span>{i<0?'GENESIS / THE CAPSTONE':studies[i].name}</span><button onClick={close} aria-label="Close experience navigation">Close ×</button></div><nav aria-label="Experience destinations"><a href="/capabilities">Capabilities <span>↗</span></a><a href="/capabilities/disciplines">All disciplines <span>↗</span></a>{i>0&&<a href={studyHref(i-1)}>Previous / {studies[i-1].name}<span>←</span></a>}{i>=0&&i<8&&<a href={studyHref(i+1)}>Next / {studies[i+1].name}<span>→</span></a>}{i>=0&&<a href="/genesis">Genesis / The capstone <span>↗</span></a>}<a href="/">Back to DWS <span>↗</span></a><a href="/#review-builder">Start with an idea <span>↗</span></a></nav><p>Original DWS demonstration. No client engagement.<br/>Illustrative systems and sample data.</p></dialog></aside>}
